<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="data.spring.mybatis.adapter.out.persistence.product.ProductEntityMapper">
    <insert id="save" useGeneratedKeys="true" keyProperty="productId">
        insert into product (product_name, price, quantity, created_at, updated_at)
        values (#{productName}, #{price}, #{quantity}, #{createdAt}, #{updatedAt})
    </insert>

    <insert id="saveAll" useGeneratedKeys="true" keyProperty="productId">
        insert into product (product_name, price, quantity, created_at, updated_at)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.productName}, #{entity.price}, #{entity.quantity}, #{entity.createdAt}, #{entity.updatedAt})
        </foreach>
    </insert>

    <select id="findById" resultType="ProductEntity">
        select product_id, product_name, price, quantity
        from product
        where product_id = #{productId}
    </select>

    <select id="findWithCond" resultType="ProductEntity">
        select product_id, product_name, price, quantity
        from product
        <where>
            <if test="productName != null and productName != ''">
                product_name like concat(#{productName}, '%')
            </if>
            <if test="maxPrice != null">
                and price &lt;= #{maxPrice}
            </if>
        </where>
    </select>

    <update id="update">
        update product
        <set>
            <if test="productName != null">product_name = #{productName},</if>
            <if test="price != null">price = #{price},</if>
            <if test="quantity != null">quantity = #{quantity},</if>
            <if test="updatedAt != null">updated_at = #{updatedAt},</if>
        </set>
        where product_id = #{productId}
    </update>

    <delete id="deleteAll">
        truncate table product restart identity
    </delete>
</mapper>
